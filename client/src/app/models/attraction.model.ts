export class Attraction {
    constructor(
        private id: string,
        private name: string,
        private date: Date,
        private link: string,
        private tripID: string,
    ) {}
}