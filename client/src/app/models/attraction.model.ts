export class Attraction {
    constructor(
        private date: Date,
        private name: string,
        private link: string,
        private tripID: string,
        private id: string
    ) {}
}